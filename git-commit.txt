9.12 封装优化切片方法

新增和优化了以下切片操作方法：

- 差集函数
- DiffSet 差集，只支持 comparable 类型（已去重，顺序不确定）
- DiffSetFunc 差集，支持自定义相等函数（已去重，顺序不确定）
- 查找函数
- Find 查找第一个满足条件的元素
- FindAll 查找所有满足条件的元素
- map方法
- deduplicateFunc 使用自定义相等函数对切片进行去重